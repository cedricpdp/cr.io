# Generate this value with: openssl rand -hex 32
POSTGRES_PASSWORD=replace-with-a-long-random-value

# Public port and published image can be overridden if needed.
CRIO_PORT=8000
CRIO_IMAGE=ghcr.io/cedricpdp/cr.io:latest
